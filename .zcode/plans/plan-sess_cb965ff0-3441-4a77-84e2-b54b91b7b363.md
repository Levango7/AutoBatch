# AutoBatch 审核问题修复计划

## 优先级 P0（阻塞上线）

### 1. 创建 CI/CD GitHub Actions 工作流
- **文件**: `.github/workflows/ci.yml`（新建）
- **内容**: lint（ruff）+ 核心测试（跳过 Spark/Cluster/Iceberg/MinIO/Benchmark）+ 覆盖率报告
- **触发**: push / pull_request 到 main

### 2. 修复测试 fixture 硬编码 Windows 路径
- **文件**: `tests/conftest.py` 第 460-468 行
- **改动**: 优先读取环境变量 `SPARK_HOME`, `JAVA_HOME`, `PYSPARK_PYTHON`, `HADOOP_HOME`，回退到当前 Windows 路径
- **范围**: `spark_cluster_env` (line 460-468), `spark_env` (line 888-897), `iceberg_env` (line 990-999)

### 3. 修复 except Exception: pass 静默吞异常
- **文件**: `src/helpers.py` 第 656-658 行
- **改动**: 添加 logging.warning 记录异常类型和消息

### 4. 修复 Spark Dockerfile 未提交修改（UID + 命令分隔符）
- **文件**: `docker/spark-cluster/Dockerfile` 第 236 行
- **改动**: 
  - UID 恢复为 1000（与 K8s securityContext 约定一致）
  - `;` 改为 `&&` 确保 useradd 失败时构建终止
  - 添加 `set -euo pipefail` 保护

### 5. 修复 PySpark pip 安装第二分支缺保护
- **文件**: `docker/spark-cluster/Dockerfile` 第 174-175 行
- **改动**: 第二分支 `pip3 install` 加 `--break-system-packages`，两分支后加 `|| exit 1`

### 6. 修复 healthcheck 虚假检查
- **文件**: `docker-compose.yml` 第 20 行
- **改动**: 改为检查配置文件存在性 + 模块导入

## 优先级 P1（重要但可后续）

### 7. 测试空壳测试补断言
- **文件**: `tests/test_error_handling.py` 第 371-399 行
- **改动**: 添加 cleanup 验证断言

### 8. 超时测试标记 xfail
- **文件**: `tests/test_error_handling.py` 第 256-281 行
- **改动**: 添加 pytest.mark.xfail 标记

## 优先级 P2（建议优化）

### 9. README 路径示例跨平台兼容
- **文件**: `readme.md`
- **改动**: 添加 POSIX 路径示例（正斜杠）

### 10. Dockerfile 基础镜像版本锁定
- **文件**: `Dockerfile` 第 8 行
- **改动**: 使用带具体 tag 的版本（如 `python:3.13.1-slim`）

### 11. 添加 .env.example 模板
- **文件**: `.env.example`（新建）
- **内容**: MinIO 凭证、Spark 配置等环境变量示例

## 修复顺序
P0 问题按文件分组批量修复，P1/P2 随后处理。